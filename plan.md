# Plan: prj-7641-task-mugt05q0

**Project:** `prj-7641-task-mugt05q0`  
**Task ID:** `task-mugt05q0`  
**Repo:** `prj-7641-task-mugt05q0`  

## Summary

Декомпозирую задачу 'Простой чат-сервер на Go' на 4 этапа: инициализация проекта, реализация ядра (websocket/hub), клиентская часть и тесты + документация.

## Subtasks

### 1. Инициализировать Go-проект и структуру

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Создать go.mod (module github.com/ZhenyaRUS39/prj-7641-task-mugt05q0), базовую структуру каталогов (cmd/server, internal/hub, internal/client, web/static), .gitignore, README.md с описанием. Версия Go 1.22+.
- **Test plan:** go.mod валиден (go mod download без ошибок), go build ./... возвращает exit 0 на пустой main, директории cmd/server, internal/hub, internal/client, web/static существуют, README содержит инструкцию запуска.

### 2. Реализовать Hub и WS-эндпоинт

- **ID:** `sub-2`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** В internal/hub: структура Hub с регистрацией/отменой регистрации клиентов и broadcast-каналом. В internal/client: Client с ReadPump/WritePump на gorilla/websocket. В cmd/server/main.go: HTTP-сервер с /ws эндпоинтом, раздача web/static, graceful shutdown.
- **Test plan:** go build ./... собирается без ошибок; go vet ./... чисто; запуск ./server слушает порт (по умолчанию 8080), GET / возвращает HTML, рукопожатие WS на /ws проходит (проверка через wscat или встроенный smoke-test).
- **Dependencies:** `sub-1`

### 3. HTML/JS клиент и стилизация

- **ID:** `sub-3`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** web/static/index.html + chat.js + chat.css: форма ввода ника, список сообщений, индикатор подключения, автоскролл. Подключение к /ws, отправка/приём JSON-сообщений формата {type:"chat"|"system", user, text, ts}.
- **Test plan:** Открыть http://localhost:8080 в браузере: ник сохраняется, ввод Enter отправляет сообщение, оно видно у всех подключённых клиентов (открыть в 2 вкладках), системные сообщения о входе/выходе отображаются, индикатор соединения корректен.
- **Dependencies:** `sub-2`

### 4. Тесты, Dockerfile, документация

- **ID:** `sub-4`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Unit-тесты для hub (регистрация, broadcast, отписка при разрыве) на testify. Dockerfile multi-stage (golang:1.22 → gcr.io/distroless/static). В README: команды запуска, переменная PORT, пример docker run, скриншот/описание протокола.
- **Test plan:** go test ./... — все тесты зелёные, покрытие internal/hub ≥70% (go test -cover). docker build -t chat-server . завершается успешно; docker run -p 8080:8080 chat-server стартует и отвечает на /ws; README содержит блоки go run / docker / env PORT.
- **Dependencies:** `sub-2`, `sub-3`


---
*Generated by Hermes Orchestrator*
